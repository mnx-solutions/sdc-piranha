'use strict';

var window = global.window;
var angular = window.angular;
var JoyentPortal = window.JoyentPortal;

(function(ng, app) {
  app.controller(
          'LoginController',
          function($scope, $http) {

          }
  );
})(angular, JoyentPortal);
